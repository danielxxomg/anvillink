.gradle/
build/
out/
*.class
*.jar
.DS_Store
.atl/
.codegraph/codegraph.db
# project GGA + opencode agent are tracked — do NOT ignore
# .gga and opencode.json stay versioned (GGA config + gga-reviewer agent)
